diff --git a/root/usr/bin/stat-genconfig b/root/usr/bin/stat-genconfig
index 7d1349b24..e5e364960 100755
--- a/root/usr/bin/stat-genconfig
+++ b/root/usr/bin/stat-genconfig
@@ -217,6 +217,8 @@ end
 function _expand( s, n, nopad )
 	local str = ""
 
+	s = luci.util.trim(s)
+
 	if type(s) == "string" then
 		for i, v in ipairs( luci.util.split( s, "%s+", nil, true ) ) do
 			str = str .. _string( v, n, nopad )
