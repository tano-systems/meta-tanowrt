--- a/networking/udhcp/dhcpc.c
+++ b/networking/udhcp/dhcpc.c
@@ -1115,7 +1115,5 @@ static void perform_renew(void)
 		state = RENEW_REQUESTED;
 		break;
 	case RENEW_REQUESTED: /* impatient are we? fine, square 1 */
-		if (allow_deconfig)
-			udhcp_run_script(NULL, "deconfig");
 	case REQUESTING:
 	case RELEASED:
