#
# Configuration to use an internal Linaro binary toolchain
#

#ILT_TARGET_SYS_arm ?= "arm-linux-gnueabihf"
#ILT_TARGET_SYS_aarch64 ?= "aarch64-linux-gnu"
#ILT_TARGET_SYS = "${TARGET_SYS}"
#TARGET_PREFIX = "${ILT_TARGET_SYS}-"

require conf/distro/include/tcmode-default.inc
